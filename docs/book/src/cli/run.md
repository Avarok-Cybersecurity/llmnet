# run

Execute a single prompt through the pipeline.

## Usage

```bash
llmnet run <COMPOSITION> <PROMPT>
```

## Example

```bash
llmnet run pipeline.json "Explain quantum computing"
```

## Options

| Option | Description |
|--------|-------------|
| `--format` | Output format: `text`, `json` |
| `--trace` | Show execution trace |
