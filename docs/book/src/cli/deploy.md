# deploy

Deploy a pipeline to a cluster.

## Usage

```bash
llmnet deploy <COMPOSITION> [OPTIONS]
```

## Options

| Option | Description |
|--------|-------------|
| `--context` | Target cluster context |
| `--replicas` | Number of replicas |
